neo4j:
  name: my-standalone
  edition: "enterprise"    
  acceptLicenseAgreement: "yes"
  password: "password"            

volumes:
  data:
    mode: defaultStorageClass
    defaultStorageClass:
      requests:
        storage: 2Gi

podSpec:
  podAntiAffinity: false
